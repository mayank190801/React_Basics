import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

//Intro of new lifecycle and shit about react
//Mounting
//Updation -- simple
//Unmounting

//some new hoook --- useEffect hook or shit something
//Second most important hook in react for sure buddy -- always remember that!!
//My css section always sucked, we will get that part later, and find some third party libraries to fix that
//My weak section, but will definitely look into it

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
