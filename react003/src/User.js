export const User = ({ username, age }) => {
  return (
    <div>
      <h2>{username}</h2>
      <h3>{age}</h3>
    </div>
  );
};

//Makes sense for sure
//we have NewUser - uske andar Form form
//UserList - works on presenting in list
// User -- display single user properly display
